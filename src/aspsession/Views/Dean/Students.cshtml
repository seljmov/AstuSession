@model IList<aspsession.ViewModels.Dean.StudentViewModel>

@{
    ViewData["Title"] = "Студенты";
    Layout = "_DeanLayout";
}

<h3>Список студентов</h3>

@if (Model == null)
{
    <p><em>Список студентов пуст...</em></p>
}
else
{
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>ФИО</th>
            <th>Номер зачетной книжки</th>
            <th>Группа</th>
            <th>Кафедра</th>
            <th>Институт</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Id</td>
                <td>@user.Name</td>
                <td>@user.BookNumber</td>
                <td>@user.Group</td>
                <td>@user.Departments</td>
                <td>@user.Institute</td>
            </tr>
        }
        </tbody>
</table>
}